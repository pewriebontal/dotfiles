test -s "$HOME/.config/func" && . "$HOME/.config/func" || true
test -s "$HOME/.config/aliasrc" && . "$HOME/.config/aliasrc" || true

# may break on different shell like ash
# FIX me later

if [ -z "$BASH" ]; then 
	source ~/.config/zsh/plugins/powerlevel10k/powerlevel10k.zsh-theme
fi

#test -s "$HOME/.cargo/env" && source $HOME/.cargo/env || true

