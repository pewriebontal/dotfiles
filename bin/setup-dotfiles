#!/bin/sh

DOTFILES_PATH="$HOME/.local/share/dotfiles"
REPO="https://github.com/pewriebontal/dotfiles.git"
dotfilesc="git --git-dir=$DOTFILES_PATH --work-tree=$HOME"

git clone --bare $REPO $DOTFILES_PATH
$dotfilesc checkout --force
$dotfilesc config --local status.showUntrackedFiles no
git clone --depth=1 https://github.com/romkatv/powerlevel10k.git ~/.local/share/powerlevel10k
